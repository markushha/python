# modules in Python work easy as hell, for example,
# let's import something from functions.py

import functions as f

# or 

from functions import mean

# and then we can use it

print(f.nroot(8, 3))
print(mean([11, 25, 66, 77, 96, 48, 32]))
